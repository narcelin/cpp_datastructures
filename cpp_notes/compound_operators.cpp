#include <iostream>
#include <string>

using namespace std;

int main(){
    int count = 0;
    int x = 1;
    count += x; // cout + x
    count -= x; // cout - x
    count *= x; // cout * x
    count /= x; // cout / 3
    count %= x; // remainder

    return 0;
}